<script>

console.log("volume!!")
  var id = <%= raw id.to_json %>

  function renderChart(exercise) {
    var ctx = document.getElementById("myChartexercise_" + id);
    var xAxis = JSON.parse($('#dateexercise_' + id).attr("value"));
    var yAxis = JSON.parse($('#volumeexercise_' + id).attr("value"));
    var myChart = new Chart(ctx, {
      type: 'line',
      data: {
          labels: xAxis,
          datasets: [{
              label: 'Volume',
              fill: true,
              data: yAxis,
              backgroundColor: [
                  'rgba(40, 44, 60, 0.2)'
              ],
              borderColor: [
                  'rgba(128, 191, 216, 1)'
              ],
              borderWidth: 1
          }]
      },
      options: {
          scales: {
              yAxes: [{
                  ticks: {
                      beginAtZero:true
                  }
              }]
          }
      }
    });
  };


  renderChart(id)

</script>
