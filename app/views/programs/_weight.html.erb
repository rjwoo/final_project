
<script>
  console.log(<%= raw id.to_json %>)
  var id = <%= raw id.to_json %>
  // var id = exericse.id
  console.log(id)
  function renderChart(exercise) {
    console.log(exercise)
    var ctx = document.getElementById("myChartexercise_" + id);
    var xAxis = JSON.parse($('#dateexercise_' + id).attr("value"));
    var yAxis = JSON.parse($('#weightexercise_' + id).attr("value"));
    var myChart = new Chart(ctx, {
      type: 'line',
      data: {
          labels: xAxis,
          datasets: [{
              label: 'Weight',
              data: yAxis,
              backgroundColor: [
                  'rgba(255, 99, 132, 0.2)',
                  'rgba(54, 162, 235, 0.2)',
                  'rgba(255, 206, 86, 0.2)',
                  'rgba(75, 192, 192, 0.2)',
                  'rgba(153, 102, 255, 0.2)',
                  'rgba(255, 159, 64, 0.2)'
              ],
              borderColor: [
                  'rgba(255,99,132,1)',
                  'rgba(54, 162, 235, 1)',
                  'rgba(255, 206, 86, 1)',
                  'rgba(75, 192, 192, 1)',
                  'rgba(153, 102, 255, 1)',
                  'rgba(255, 159, 64, 1)'
              ],
              borderWidth: 1
          }]
      },
      options: {
          scales: {
              yAxes: [{
                  ticks: {
                      beginAtZero:true
                  }
              }]
          }
      }
    });
  };


  renderChart(id)

</script>
