Spotter is a web application built on Ruby on Rails.

The front-end is a combination of Bootstrap and CSS, utilizing multiple libraries such as Unicorn UI and Font Awesome. The chart library used for the charts on the show page for the programs is Chart.js. jQuery is used for the exercise logs once a workout has been started.

The back-end is built with Rails and uses a postgreSQL database.
